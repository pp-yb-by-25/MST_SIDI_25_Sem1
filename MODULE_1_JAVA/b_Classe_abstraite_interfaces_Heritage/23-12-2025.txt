🧠 Partie 1 : QCM – Questions théoriques

Q1. Une classe abstraite en Java :
a) Peut être instanciée directement
b) Peut contenir des méthodes abstraites et concrètes  <-----------
c) Ne peut pas avoir d’attributs
d) Ne peut pas avoir de constructeur

Q2. Une interface en Java :
a) Peut contenir des attributs non final
b) Peut être instanciée
c) Peut être implémentée par plusieurs classes  <---------------------
d) Peut hériter d’une classe

Q3. Quelle est la bonne syntaxe pour qu’une classe hérite d’une classe abstraite Transport ?
a) class Voiture implements Transport
b) class Voiture extends Transport <-------------------------
c) class Voiture inherits Transport
d) class Voiture uses Transport

Q4. Une classe Java peut :
a) Hériter de plusieurs classes
b) Implémenter plusieurs interfaces <--------------------
c) Hériter d’une interface
d) Implémenter une classe abstraite

Q5. À quoi sert principalement une interface ?
a) À stocker des données
b) À imposer un comportement commun <------------------------------
c) À créer des objets
d) À remplacer les classes abstraites

💻 Partie 2 : Mini-exercice de programmation (Java)
📌 Énoncé

//Créer une classe abstraite Transport avec :
public abstract class Transport
{
    //un attribut matricule
    private String matricule;

    //un constructeur
    public Transport(String matricule)
    {
        this.matricule = matricule;
    }

    //Getters & Setters
    public String getMatricule()
    {
        return this.matricule;
    }
    public void setMatricule(String matricule)
    {
        this.matricule = matricule;
    }
    //

    //une méthode abstraite calculerVitesse()
    public abstract double calculerVitesse();
    //

    //une méthode concrète afficherInfo()
    public String afficherInfo()
    {
        return "Matricule du transport est : "+this.matricule;
    }
}

//Créer une interface Motorise qui contient :
public interface Motorise
{
    //une méthode demarrer()
    public void demarrer();

}

//Créer deux classes :
//Ces classes doivent :
//hériter de Transport
//implémenter l’interface Motorise
//redéfinir toutes les méthodes nécessaires

//Voiture
public class Voiture extends Transport implements Motorise
{
    //Attributs
    private double vitesse;

    //constructeur
    public Voiture(String matricule , double vitesse)
    {
        super(matricule);
        this.vitesse = vitesse;
    }

    //Getters
    public double getVitesse()
    {
        return this.vitesse;
    }

    //Methode abstraite
    @Override
    public double calculerVitesse()
    {
        return this.vitesse;
    }
    @Override
    public void demarrer()
    {
        System.out.println("La voiture se démarre");
    }
    //Methdoes polymorphisme
    @Override
    public String afficherInfo()
    {
        return "La voiture est de matricule :"+this.getMatricule() + 
                "elle roule à vitesse de : "+this.vitesse;
    }
}

//Camion
public class Camion extends Transport implements Motorise
{
    //Attributs
    private double vitesse;
    private double capacite;
    private final double MAX_CAPACITE = 500;
    //

    //constructeur
    public Camion(String matricule , double capacite ,  double vitesse)
    {
        super(matricule);
        this.vitesse = vitesse ;
        this.capacite = capacite;
    }

    //Getters & Setters
    public double getVitesse()
    {
        return this.vitesse;
    }
    public final double getMaxCapacite()
    {
        return this.MAX_CAPACITE;
    }
    //

    //Methdoes abstraites
    @Override
    public double calculerVitesse()
    {
        return this.vitesse;
    }
    @Override
    public void demarrer()
    {
        System.out.println("Le camion se démarrer");
    }
    //
    //Methodes polymorphisme
    @Override
    public String afficherInfo()
    {
        return  "Le camion est de vitesse : "  + this.vitesse
                + " et de matricule : " + this.getMatricule();
    }
    //
    public boolean surcharger(double valeur)
    {
        if(this.capacite + valeur <= this.MAX_CAPACITE)
        {
            return true;
        }
        return false;
    }

}

// Créer une classe TestTransport avec une méthode main qui :
public class Main
{
    public static void main(String [] args)
    {
        //crée une Voiture et un Camion
        Transport v1 = new Voiture("12F134" , 120.00);
        Transport c1 = new Camion("000ABC" , 80.50 , 300);
        //

        //appelle leurs méthodes pour démontrer le polymorphisme
        Voiture v1_cast = (Voiture) v1;
        
        System.out.println(v1_cast.calculerVitesse());
        System.out.println(v1_cast.afficherInfo());
        System.out.println(v1_cast.getVitesse());
        //
        Camion c1_cast = (Camion) c1;

        System.out.println(c1_cast.calculerVitesse());
        System.out.println(c1_cast.afficherInfo());
        System.out.println(c1_cast.surcharger(300) + " Car la capacité maximale est : " + c1_cast.getMaxCapacite());

        //
       
    }
}

//